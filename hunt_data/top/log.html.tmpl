<!DOCTYPE html>
<html>
<head>
<title>Team Log</title>
<link rel="stylesheet" type="text/css" href="log.css">
</head>
<body>
team: {{ team.name }}<br>
<pre>
{% for entry in log_entries %}{{ entry.timestamp|date:'Y-m-d H:i:s' }} {% if entry.link %}<a href="{{ root }}{{ entry.link }}">{% endif %}{{ entry.message }}{% if entry.link %}</a>{% endif %}
{% endfor %}
</pre>
</body>
</html>
